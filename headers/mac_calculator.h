//#include <iostream>
//#include <vector>
//#ifndef MACCALCULATOR_H
//#define MACCALCULATOR_H
//#include "hpacket.h"
//#include "ff.h"


//class macCalculator
//{
  //  public:
   //std::vector<uint8_t> MAC;
   //std::vector<std::vector<uint8_t>> publickeyset = {{1,2,3},{2,3,4},{3,4,5},{4,5,6},{5,6,7}};

//void maccalculator(std::vector<uint8_t> &_hcodedSymbol,std::vector<std::vector<uint8_t>> &_setOFpublickeys){


  //  _setOFpublickeys = publickeyset;
    //    ff ff(256);
       // _hcodedSymbol.resize(this->packetsize);
         //std::fill(_hcodedSymbol.begin(), _hcodedSymbol.end(), 0);

     //for (uint8_t i = 0; i < MAC.size(); i++){
    //std::vector<uint8_t> tag(this->symbol_size, 0);
        //for(uint8_t j=0; j<_setOFpublickeys[i].size(); j++){

        //  First, multiply coefficients[i] with uncodedSymbols[i] and put result in temp
      //     std::vector<uint8_t> MACC = ff.v2vMulipllication(_setOFpublickeys[i], _hcodedSymbol);
        //   MAC[i]=  MACC[1];
        //} 
        // Second, add temp with codedSymbols and put result in codedSymbols
        //MAC[i] = ff.division(MAC[i], _setOFpublickeys[i].back() );
//};
  
//};
//};


//#endif